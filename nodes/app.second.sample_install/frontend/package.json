{
  "name": "second-pwa",
  "version": "0.1.0",
  "private": true,
  "homepage": "/app/app.second.sample_pwa",
  "dependencies": {
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-scripts": "2.1.5",
    "http-server": "0.11.1",
    "react-art": "16.8.4",
    "react-native-web": "0.10.0",
    "semver": "5.6.0",
    "flatted": "2.0.0"
  },
  "scripts": {
    "start": "node src/update-version.js && cp src/version.json public/version.json && react-scripts build",
    "second:build:self": "node src/update-version.js && cp src/version.json public/version.json && react-scripts build",
    "repl:start": "node src/update-version.js && cp src/version.json public/version.json && react-scripts build && http-server build -p 3000",
    "dev": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
